import numpy as np


class InteriorPoint:
    # Given initial point
    def maximize(self, *, A:np.matrix, c:np.matrix, b:np.matrix, alpha:float=0.5, eps:float=0.001, x:np.matrix):
        #TODO
        return

    # Not given initial point
    def maximize(self, *, A:np.matrix, c:np.matrix, b:np.matrix, alpha:float=0.5, eps:float=0.001):
        #TODO
        pass
    
    # Given initial point
    def minimize(self, *, A:np.matrix, c:np.matrix, b:np.matrix, alpha:float=0.5, eps:float=0.001, x:np.matrix):
        #TODO
        pass

    # Not given initial point
    def minimize(self, *, A:np.matrix, c:np.matrix, b:np.matrix, alpha:float=0.5, eps:float=0.001):
        #TODO
        pass
    
    def findInitialPoint(self, *, A:np.matrix, b:np.matrix):
        #TODO
        pass  



if __name__ == "__main__":
    #TODO
    pass